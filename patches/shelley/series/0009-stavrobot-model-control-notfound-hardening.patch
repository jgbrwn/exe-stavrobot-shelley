diff --git a/server/handlers.go b/server/handlers.go
index 5fa77fb..667ab46 100644
--- a/server/handlers.go
+++ b/server/handlers.go
@@ -1162,17 +1162,8 @@ type stavrobotModelControlApplyRequest struct {
 	Model string `json:"model"`
 }
 
-var errStavrobotModelControlConversationNotFound = errors.New("stavrobot_model_control_conversation_not_found")
-
 func (s *Server) loadConversationManagerForStavrobotModelControl(ctx context.Context, conversationID string) (*ConversationManager, error) {
-	manager, err := s.getOrCreateConversationManager(ctx, conversationID, "")
-	if err != nil {
-		if errors.Is(err, sql.ErrNoRows) {
-			return nil, errStavrobotModelControlConversationNotFound
-		}
-		return nil, err
-	}
-	return manager, nil
+	return s.getOrCreateConversationManager(ctx, conversationID, "")
 }
 
 func (s *Server) handleStavrobotModelControlView(w http.ResponseWriter, r *http.Request, conversationID string) {
@@ -1184,7 +1175,7 @@ func (s *Server) handleStavrobotModelControlView(w http.ResponseWriter, r *http.
 	ctx := r.Context()
 	manager, err := s.loadConversationManagerForStavrobotModelControl(ctx, conversationID)
 	if err != nil {
-		if errors.Is(err, errStavrobotModelControlConversationNotFound) {
+		if strings.Contains(err.Error(), "conversation not found") {
 			http.Error(w, "Conversation not found", http.StatusNotFound)
 			return
 		}
@@ -1224,7 +1215,7 @@ func (s *Server) handleStavrobotModelControlApply(w http.ResponseWriter, r *http
 	ctx := r.Context()
 	manager, err := s.loadConversationManagerForStavrobotModelControl(ctx, conversationID)
 	if err != nil {
-		if errors.Is(err, errStavrobotModelControlConversationNotFound) {
+		if strings.Contains(err.Error(), "conversation not found") {
 			http.Error(w, "Conversation not found", http.StatusNotFound)
 			return
 		}
diff --git a/server/stavrobot_model_control_test.go b/server/stavrobot_model_control_test.go
index 2957e8a..a2d1540 100644
--- a/server/stavrobot_model_control_test.go
+++ b/server/stavrobot_model_control_test.go
@@ -96,6 +96,18 @@ func createConversationWithOptions(t *testing.T, h *TestHarness, opts db.Convers
 	return conv.ConversationID
 }
 
+func TestHandleStavrobotModelControlViewConversationNotFound(t *testing.T) {
+	h := NewTestHarness(t)
+
+	req := httptest.NewRequest(http.MethodGet, "/api/conversation/missing/stavrobot/model-control-view", nil)
+	w := httptest.NewRecorder()
+	h.server.handleStavrobotModelControlView(w, req, "missing")
+
+	if w.Code != http.StatusNotFound {
+		t.Fatalf("expected status %d, got %d: %s", http.StatusNotFound, w.Code, w.Body.String())
+	}
+}
+
 func TestHandleStavrobotModelControlViewRejectsNonStavrobotConversation(t *testing.T) {
 	h := NewTestHarness(t)
 	conversationID := createConversationWithOptions(t, h, db.ConversationOptions{})
@@ -229,6 +241,18 @@ func TestHandleStavrobotModelControlApplyMissingModel(t *testing.T) {
 	}
 }
 
+func TestHandleStavrobotModelControlApplyConversationNotFound(t *testing.T) {
+	h := NewTestHarness(t)
+	req := httptest.NewRequest(http.MethodPost, "/api/conversation/missing/stavrobot/model-control-apply", bytes.NewBufferString(`{"model":"openrouter/model-a"}`))
+	req.Header.Set("Content-Type", "application/json")
+	w := httptest.NewRecorder()
+	h.server.handleStavrobotModelControlApply(w, req, "missing")
+
+	if w.Code != http.StatusNotFound {
+		t.Fatalf("expected status %d, got %d: %s", http.StatusNotFound, w.Code, w.Body.String())
+	}
+}
+
 func TestHandleStavrobotModelControlApplyRejectsNonStavrobotConversation(t *testing.T) {
 	h := NewTestHarness(t)
 	conversationID := createConversationWithOptions(t, h, db.ConversationOptions{})
